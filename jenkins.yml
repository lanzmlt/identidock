identidock:
  build: .
    dockerfile: Dockerfile
  expose:
    - "9090"
  environment:
    ENV: PROD
  links:
    - dnmonster
    - redis

dnmonster:
  image: amouat/dnmonster

redis:
  image: redis
